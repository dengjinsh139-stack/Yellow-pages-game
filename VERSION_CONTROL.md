# 版本管理规范

## 版本号规则 (Semantic Versioning)

格式: `vMAJOR.MINOR.PATCH`

| 部分 | 说明 | 递增时机 |
|------|------|----------|
| MAJOR | 主版本 | 重大功能变更、架构重构、不兼容修改 |
| MINOR | 次版本 | 新增功能、新游戏、新页面 |
| PATCH | 补丁版本 | Bug修复、代码清理、优化、文档更新 |

## 版本标签管理

### 当前版本标签

| 标签 | 说明 | 提交 | 时间 |
|------|------|------|------|
| `v2.0.1` | 代码清理 | `97c4f10` | 2026-03-17 |

### 回滚操作

如需回滚到某个版本：

```bash
# 方法1: 本地回滚到标签
git checkout v2.0.1

# 方法2: 强制回滚到标签（危险！会丢失后续提交）
git reset --hard v2.0.1
git push -f origin main

# 方法3: 创建回滚提交（推荐）
git revert HEAD~3..HEAD  # 回滚最近3个提交
git push origin main
```

## 版本发布流程

1. **开发完成** → 本地测试通过
2. **更新版本号** → 修改 VERSION.md
3. **提交代码** → git commit
4. **打标签** → git tag vX.Y.Z
5. **推送标签** → git push origin vX.Y.Z
6. **自动部署** → GitHub Pages 自动更新

## 版本历史

### v2.0.1 (2026-03-17)
- 删除冗余文件5个
- 节省空间200KB
- 文件数37→32

### v2.0.0 (2026-03-10)
- 工业风格改版
- 5个核心音游
- 20个新游戏

---

**回滚须知**: 每次打标签的代码都可随时回滚。重要操作前先打标签！

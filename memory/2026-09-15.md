# 2026-09-15

## 00:24 - 智能备份 + 修复 GitHub 推送故障 (cron)

### 备份执行
- cron 触发 `smart_backup.py`，检测到新内容
- ✅ 本地备份: `Kimi_Claw_记忆备份_2026年09月15日_00时24分.tar.gz` (141.7 MB)
- ✅ 版本号: v3.75.4 → v3.75.5（后随修复提交再到 v3.75.6）
- ✅ 旧备份清理（保留10个）

### 发现并修复: smart_backup.py GitHub 推送失败
- **症状**: `could not read Username for 'https://github.com'` — push 失败
- **根因**: `~/.git-credentials` 为空（0字节，7月4日起），smart_backup.py 用裸 `git push`，无认证
- **对比**: `backup.sh` 内嵌 PAT token（临时 set-url 带 token 推送，推完恢复干净 URL），所以 23:51 的备份推送成功
- **修复**: 给 smart_backup.py 的 `push_to_github()` 加了 `get_github_token()`（从 backup.sh 解析 token，保持单一来源），推送前临时 set-url 带认证地址、推送后恢复干净 URL；同时把首次推送目标从不存在的 `master` 分支改为 `main`，并改为无论是否有新提交都尝试推送（避免漏推积压提交）
- **验证**: 手动调用修复后的 push 成功，remote URL 已恢复不含 token，本地与远程 0 提交差距
- 教训: token 轮换只需改 backup.sh 一处

### 状态
- GitHub 远程最新提交: ca4b48c (v3.75.6)，与本地完全同步

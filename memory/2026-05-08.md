# 2026-05-08

## 乐器语言合成器 - 乐器入库规则（完整版）

用户邓锦盛制定了详细的乐器入库标准，用于 sound-effects/instrument-language-synth.html 新增乐器时遵循。

### 核心原则
1. **不破坏三栏骨架**：`.workbench` 必须保持 grid、3个子元素、`left-panel`/`center-panel`/`right-panel`
2. **不破坏现有功能**：文字演奏、MIDI、语义触发、角色声纹、两种映射模式、Expression Segment、实时/WAV导出
3. **不要随机不稳定**：legacy-tone-region 禁止 Math.random()，必须稳定映射

### 新乐器配置标准
- 必备字段：id, name, englishName, type, category, basePath, defaultVolume, attack, release
- dialogueCoreNotes: ['A4','B4','D5','E5']
- legacyRequiredNotes: D4,E4,F4,A4,B4,D5,E5,F5,A5,B5
- fallbackPolicy: 'nearest-sample-preferred'（缺采样优先 nearest sample 变调，不直接切 synth）

### 必须接入的播放路径
1. 文字演奏：playText() → textToEvents() → charToNote() → playTone()
2. WAV导出：renderAudioToBuffer() → exportWav()（实时和导出用同一逻辑）
3. 歌曲MIDI
4. 语义触发MIDI
5. 角色声纹：characterVoiceConfig / characterVoiceRegistry / playCharacterVoice()

### Race Condition 防护（P0历史问题）
- 切乐器时立即记录 pendingInstrument / selectedInstrument
- 播放前检查 core notes 是否 ready
- 未 ready 禁用按钮或 await ensureInstrumentCoreReady()
- 不能只做 await loadInstrumentSamples(); currentInstrument = instrument;

### 交付时必须按格式汇报
新增乐器列表 → 采样覆盖情况 → 已验证功能 → LayoutGuard结果 → 已知限制

## 水滴合成器完整入库流程（标准范例）

### v3.23.7：小号重命名为水滴合成器
- 🎺小号 → 💧水滴合成器
- CSS 颜色从金色 #e9c46a → 水滴蓝 #00b4d8
- 内部 data-instrument="trumpet" ID 保持不变（后续才改）

### v3.23.12：接入真实采样（P0 错误：ID 不一致）
- UI data-instrument="trumpet"，但采样/loader 用 waterdrop
- 导致 loadInstrumentSamples('trumpet') 无 loader → 采样永不加载
- **教训：ID 必须统一！**

### v3.24.1：P0 修复 - canonical ID 统一为 waterdrop
- 所有 trumpet 统一为 waterdrop（22处替换）
- playSampleTone / renderSampleToBuffer 移除特殊 case
- hasInstrumentSamples('waterdrop') 检查 waterdrop_D4
- instrumentDefaults.waterdrop 默认参数
- **新增 INSTRUMENT_REGISTRY** - 所有乐器唯一 canonical ID 注册表
- normalizeInstrumentId('trumpet') → 'waterdrop'（向后兼容旧存档）

### 正确的乐器入库步骤（以后必须按此执行）：
1. UI data-instrument="instrument_id"
2. INSTRUMENT_REGISTRY 注册
3. instrumentDefaults[instrument_id] 默认参数
4. sampleConfig[instrument_id] 采样配置（到位后）
5. loadInstrumentSamples 注册 loader
6. hasInstrumentSamples(instrument_id) 检查
7. instrumentOffsets / voiceRegistry / noteVolumes / chordVolumes 注册
8. instrumentColors / activeColors 主题色
9. CSS 变量 --instrument_id
10. playTone 中注册采样分支（或合成器 fallback）
11. isSampleInstrument 加入导出路径
12. waveTypeMap 注册
13. 所有名称映射（instrumentNames / exportNames / statusNames）

## v3.24.1：legacy-tone-region 早期音区修复

**根因**：createLegacyToneRegionNoteEvent 遇到轻声返回 type='pause'，导致 legacy note 数量少于 dialogue-fixed。

**修复**：
```javascript
function createLegacyToneRegionNoteEvent(char, tone, pentatonicFreqs, baseDuration) {
    // 1. 先创建对白 note 事件（保持 duration/velocity/char 一致）
    const evt = createDialogueNoteEvent(char, dialogueTone, baseDuration);
    
    // 2. 替换音高为 legacy-tone-region 映射
    const availableNotes = legacyRegion.rest ? ['D4','E4','F4'] : legacyRegion.notes;
    const hash = (char.charCodeAt(0) + tone * 31) % availableNotes.length;
    evt.note = selectedNote;
    evt.frequency = pentatonicFreqs[selectedNote];
    evt.sampleNote = findNearestSample(selectedNote);
    evt.mappingMode = 'legacy-tone-region';
    return evt;
}
```

**验收标准**：
- fixed.length === legacy.length ✅
- fixed.map(e => e.char) 与 legacy.map(e => e.char) 一致 ✅
- legacy 连续两次 note 序列完全一致（stable hash）✅

## v3.24.3：嘴巴小号占位结构
- UI 添加 💧 嘴巴小号按钮 (data-instrument='mouthtrumpet')
- INSTRUMENT_REGISTRY 注册 mouthtrumpet
- 所有映射、颜色、音量、waveform 已注册
- playTone 中暂时走 playSynthTone（等采样到位后切换为采样）
- **等待用户发送 10 个采样文件（D4~B5）**

## 关键教训
1. **永远不要 UI 用一个 ID，config 用另一个 ID** — 这是 P0
2. **INSTRUMENT_REGISTRY 是唯一真理来源** — 禁止手写 instrument id
3. **isSampleInstrument 列表必须同步更新** — 否则导出变 synth
4. **legacy-tone-region 不能返回 pause** — 必须和 fixed 数量一致
5. **每次新增乐器后必须跑完整 QA 清单**

---

## 下午 session：三大乐器采样入库 + 参数调整 + Auto Curve V1

### 卡林巴(Kalimba)采样入库 — v3.24.8
- 10个真实采样：D4/E4/F4/A4/B4/D5/E5/F5/A5/B5
- 补全 sampleConfig.kalimba + loadKalimbaSamples() 真实加载逻辑
- 原占位函数已替换

### Small机器人采样入库 — v3.24.10
- 10个真实采样同上
- 补全 sampleConfig.smallrobot + loadSmallrobotSamples() 真实加载逻辑

### Big机器人采样入库 — v3.25.0
- 10个真实采样同上
- 补全 sampleConfig.bigrobot + loadBigrobotSamples() 真实加载逻辑
- PATCH ≥ 40 触发 MINOR 进位 (v3.24.x → v3.25.0)

### 默认参数统一调整
- **发音密度** (v3.25.1)：所有乐器 notesPerBeat: 2 → 1（synth 保持 8）
- **混响** (v3.25.2)：全局 0.2 → 0，所有乐器默认混响全部归零

### Expression Segment Auto Curve V1 — v3.25.3
- 新增 `autoCurveExpressionSegments()` 函数
- 分析维度：标点(疑问/惊叹/省略/逗号/分号/句号)、关键词(命令/危险/安慰/犹豫)、重复字、段长度
- V1 保守边界：tempoScale 0.75~1.25 / velocityScale 0.75~1.30 / pauseAfterMs 0~800
- 手动覆盖机制：`updateSegment()` 标记 `_manual=true`，再次自动生成时保留手动值
- UI 增加 ⚡自动 / ✏️手动 标识
- 复用现有 `applyExpressionSegments` 链路（实时播放 + WAV 导出）

### 已知问题（未修复）
- `handleMidiFile` 函数重复定义（第6503行和第8219行）
  - 第一个是 MIDI 文件输入处理器，第二个是歌曲 MIDI 加载器
  - 第二个会覆盖第一个，但参数签名兼容，暂未导致明显故障
  - 标记为待处理，需专门 review MIDI 模块后再修

### 代码规模
- instrument-language-synth.html 当前约 **8718 行**
- CSS ≈ 2000+ 行 / HTML ≈ 800 行 / JS ≈ 5800 行

### 当前版本
**v3.25.3**

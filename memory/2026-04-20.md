# 2026-04-20 工作记录

## 乐器语言合成器 - 标点符号气口系统（v3.5.0 → v3.6.13）

### 上午：标点符号气口系统设计
- 用户提供了完整的标点符号气口设计规格
- 三层停顿体系：空格(0.25拍) / 短气口(逗号/顿号/分号/冒号) / 句尾落点(句号/问号/感叹号/省略号)
- BPM自适应、前后音修饰、统一事件模型（note + pause）

### 下午：实现与修复

#### v3.6.5 - 标点符号气口系统
- 新增 punctuationRules 配置（17种标点）
- 新增 phrasingConfig 可调参数
- tokenizeText() 分词 + getPunctuationRule() BPM自适应
- textToNotes() 统一事件模型
- 实时播放支持 pause 事件

#### v3.6.7 - 修复高亮错位与漏音
- playText() 改用真实事件索引 i 驱动高亮
- noteDuration 增加最短可听下限 110ms（后改为 BPM 动态计算）
- velocityMultiplier 仅对 noteDuration >= 160ms 生效
- 四声默认值从 32 改为 16 分音符
- 音符时长滑块文案改为"音色衰减/包络长度"

#### v3.6.8 - 最短可听下限 BPM 动态
- minAudibleMs = (60000 / bpm) * 0.23

#### v3.6.9 - WAV 导出修复（钢琴静音）
- 修复钢琴导出 buffer key 错误（piano_D4 → D4）
- 统一节奏引擎：computeEventTiming() 共用
- calculateAudioDuration() 和 renderAudioToBuffer() 共用同一函数
- checkBufferIsSilent() 静音检测

#### v3.6.10 - WAV 导出采样响度修复
- renderSampleToBuffer() 对齐实时播放
- 应用 sampleInfo.gain
- 完整 envelope（attack/hold/decay）
- 应用 instrumentOffsets
- source.stop() 留 0.5s 尾部余量

#### v3.6.12 - 木琴导出采样分支
- xylophone 加入 renderSampleToBuffer 导出路径

#### v3.6.13 - 导出混响修复
- 所有导出路径添加 wet 混响链（delay + feedback + wet gain）
- calculateAudioDuration() 为混响尾巴增加 1.5s

### 版本号问题
- 发现 pre-commit hook 自动递增版本号（nav-config.js）
- 手动写的版本号（VERSION.md/MEMORY.md）与实际不一致
- 已统一按 hook 输出的版本号记录

### GitHub 推送
- 今日共推送 5 次 commit
- 最新：v3.6.13

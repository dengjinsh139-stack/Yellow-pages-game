# 2026-05-04 工作日志

## Voice Shaping 系统

### v3.15.0 (2026-05-04)
- 角色声纹 WAV 导出脱离实时 audioContext 依赖
- loadCharacterVoiceAudio() 新增 decodeContext 参数

### v3.15.1
- phraseMidi 导出：MIDI 数字(62/64等)通过 noteNumberToName() 转为 D4/E4 等字符串音名
- 修复 findNearestSample() 防御性类型检查

### v3.15.2
- phraseMidi 导出乐器判断加入 guitar 虚拟人声 (isVirtualVoice)

### v3.15.3
- 隐藏导航栏：📁 项目 / 📊 项目能力 / 💰 理财相关（永久移除，只保留首页/游戏/音频/任务管理器）

### v3.15.4
- Voice Shaping 完整实现：
  - voiceRegistry + characterVoiceRegistry + resolveVoiceShaping()
  - 6 个塑形参数：HPF / Mud Cut / High Shelf / Attack / Release / Overlap
  - A/B 对比 + Preset 按乐器保存/加载
  - 实时播放、和弦、语义 MIDI、角色声纹、WAV 导出共用同一套参数

### v3.15.5
- Voice Shaping QA 修复：
  - overlap 方向修正（让音符提前进入，不是增加空隙）
  - 角色真实音频接入 Voice Shaping EQ+envelope
  - resolveVoiceShaping() 继承顺序：global < voiceDefault < characterOverride
  - A/B 播放等待完整播放结束后再恢复参数

### v3.15.6
- UI 面板重组：
  - 主界面：乐器选择 → 当前 Voice 徽章 → 基础参数(BPM/发音密度/音量) → Voice Shaping → 空间/Effects(混响) → 导出
  - 高级区折叠：声调时值映射、实验性节奏随机⚠️、Sample Trim、和弦长度
  - Legacy 区折叠：播放间隔、包络长度（提示已被 Voice Shaping 覆盖）
  - 频谱显示移到中间面板音符序列下方

## 待办
- [ ] v3.15.6 QA 剩余 3 项修复（待推送）：
  1. testPlayText() 返回 undefined → 改为 return playText()
  2. calculateAudioDuration() double count → 改为时间线算法 (currentTime + lastAudioEndTime)
  3. UI 重复区块：空间/Effects 和导出按钮各出现 2 次

## 约定
- 每次 git push 后必须主动报出版本号
- 导航栏隐藏项（projects/caps/finance）已记入 MEMORY.md，后续更新 nav-config.js 时需检查不再恢复


# 2026-04-24 工作日志

## Yellow-pages-game 语义 MIDI 系统

### 版本迭代
- v3.7.0 → v3.11.x (多次迭代)
- 当前: v3.11.8 (语义 MIDI 导出修复)

### 核心改动
1. **新增 semantic-player.js** — MIDI 解析器 + 语义播放器
2. **新增 7 个语义 MIDI** (semantic_01-07.mid)
   - 01: 感谢 | 02: 打招呼 | 03: 道歉 | 04: 请求
   - 05: 告别 | 06: 赞美 | 07: 确认
3. **修复启动崩溃** — 函数名冲突 + 404 阻断
4. **UI 重构** — 删除快速预设，替换为语义分类触发面板
5. **实时检测** — 语义分类 + 角色声纹实时 badge 提示
6. **导出修复** — phraseMidi 使用与实时播放一致的采样渲染
7. **高亮效果** — phraseMidi 青色发光 + 右侧卡片闪烁反馈

### 待确认问题
- 木琴 xylophone 播放时有 8bit 合成音色混入（已加调试日志，等待用户反馈）
- 可能原因：某个音符采样加载失败回退到合成器

### 技术要点
- `phraseTriggerCategories` 只保留有 MIDI 文件的分类
- `textToEvents()` 只匹配 `parsedMidi` 存在的分类
- `renderAudioToBuffer()` 中 phraseMidi 使用 `renderSampleToBuffer` / `renderSynthToBuffer`
- 情绪系统 (`emotionKeywords`) 与语义系统 (`phraseTriggerCategories`) 并行运作
